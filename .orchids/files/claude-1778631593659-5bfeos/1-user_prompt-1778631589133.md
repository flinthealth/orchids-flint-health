
Content Bridge — Desktop Landscape Layout (content-bridge.tsx)
Replace the entire @media (min-width: 1200px) block with this:
css@media (min-width: 1200px) {
  .cb-photo-wrapper {
    max-width: 960px;
    margin: 0 auto;
    position: relative;
    width: 100%;
  }

  .cb-photo {
    height: 420px;
    width: 100%;
  }

  .cb-photo img {
    object-position: center 25%;
  }

  .cb-bleed {
    position: absolute; inset: 0;
    height: 100%;
    background: none;
    -webkit-mask-image: none;
    mask-image: none;
    pointer-events: none;
  }

  .cb-vignette-sides {
    display: block;
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, #3d4d58 0%, transparent 22%, transparent 72%, #a0522d 100%);
    pointer-events: none;
    z-index: 2;
  }

  .cb-vignette-bottom {
    display: block;
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 55%;
    background: linear-gradient(to bottom, transparent 0%, rgba(61,77,88,0.4) 50%, rgba(70,65,55,0.85) 80%, rgba(75,65,50,0.97) 100%);
    pointer-events: none;
    z-index: 2;
  }

  .cb-text-on-photo {
    display: block;
    position: absolute;
    bottom: 0; left: 0; right: 0;
    padding: 0 56px 28px;
    text-align: left;
    z-index: 20;
    max-width: 960px;
    margin: 0 auto;
  }

  .cb-text-on-photo h2 {
    font-size: 52px;
    max-width: 640px;
    margin: 0 0 12px;
    text-align: left;
  }

  .cb-text-on-photo p {
    font-size: 20px;
    text-align: left;
  }

  .cb-continuation {
    display: block;
    background: linear-gradient(to right, #3d4d58 0%, #6b4b3e 55%, #a0522d 100%);
    margin-top: -2px;
    padding: 20px 0 56px;
    text-align: center;
  }

  .cb-continuation p {
    font-size: 18px;
    max-width: 480px;
    margin: 0 auto;
  }

  .cb-text-desktop { display: none; }
}
Also in the JSX, inside the @media (min-width: 1200px) section add two new overlay divs inside the photo wrapper — one for side vignette, one for bottom vignette — both with classes cb-vignette-sides and cb-vignette-bottom. These are display: none on mobile and tablet via their default styles.
Add to default CSS:
css.cb-vignette-sides { display: none; }
.cb-vignette-bottom { display: none; }
Desktop only (min-width: 1200px). Mobile and tablet unchanged.