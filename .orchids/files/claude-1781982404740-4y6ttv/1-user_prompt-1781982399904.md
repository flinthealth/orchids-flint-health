File: src/components/sections/serious-about-series.tsx
Replace the entire contents of this file with the code below. Do not change the file path or the component name. This removes the comparison table from this file, since it's relocating to the FAQ section in a separate change.

"use client";

import React from 'react';

export default function SeriousAboutSeries() {
  return (
    <section style={{ background: '#f9f5ef' }}>
      <div className="max-w-[1000px] mx-auto px-6 md:px-12 py-20">
        <div className="flex flex-col lg:flex-row lg:items-start lg:justify-between gap-10">

          {/* Text column */}
          <div className="lg:max-w-[560px]">
            <p className="text-[15px] font-semibold tracking-[0.1em] uppercase text-[#677283] mb-4">
              SERIOUS ABOUT SERIES
            </p>
            <h2 className="text-[28px] md:text-[36px] font-light text-[#2b3335] leading-[1.2] tracking-[-0.01em] mb-5">
              Flint is a full-service production partner. We make it easy for healthcare brands to develop, launch, and grow branded series across all channels.
            </h2>
            <p className="text-[17px] text-[#43382f] leading-relaxed opacity-90">
              A series is flexible in the best way. It can build out into seasons and travel across every stage of the patient, provider, or employee journey.
            </p>
          </div>

          {/* Image column — reserved space only. Add the chosen image here in Orchids once the asset is finalized. */}
          <div className="hidden lg:block lg:w-[360px] lg:flex-shrink-0">
            {/* image placeholder */}
          </div>

        </div>
      </div>
    </section>
  );
}

This must render correctly at all breakpoints: below 768px, the 768px md breakpoint, and the 1200px lg breakpoint. Below the lg breakpoint, the right column should not appear at all, just the single text column.
File: src/components/sections/content-bridge.tsx
Do not change anything else in this file. Make these three changes only, each replaces a checklist with a single sentence.

Change 1 — find this exact block:
          <ul className="cb-body-on-photo" style={{ color: 'rgba(249,245,239,0.72)', lineHeight: 1.8, marginTop: '12px', listStyle: 'none', padding: 0 }}>
            {['Hand-picked experts', 'Evidence-based insights', 'Compelling stories', 'A clear narrative arc'].map((item) => (
              <li key={item} style={{ display: 'flex', alignItems: 'baseline', gap: '8px' }}>
                <span style={{ color: '#ff7f29', flexShrink: 0 }}>✓</span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
Replace with:
          <p className="cb-body-on-photo" style={{ color: 'rgba(249,245,239,0.72)', lineHeight: 1.6, marginTop: '12px', maxWidth: '460px' }}>
            From the experts and evidence-based insights you bring into the story, every detail stays true to your message.
          </p>

Change 2 — find this exact block:
        <ul style={{ color: 'rgba(249,245,239,0.72)', fontSize: '17px', lineHeight: 1.8, listStyle: 'none', padding: 0 }}>
          {['Hand-picked experts', 'Evidence-based insights', 'Compelling stories', 'A clear narrative arc'].map((item) => (
            <li key={item} style={{ display: 'flex', alignItems: 'baseline', gap: '8px' }}>
              <span style={{ color: '#ff7f29', flexShrink: 0 }}>✓</span>
              <span>{item}</span>
            </li>
          ))}
        </ul>
Replace with:
        <p style={{ color: 'rgba(249,245,239,0.72)', fontSize: '17px', lineHeight: 1.6 }}>
          From the experts and evidence-based insights you bring into the story, every detail stays true to your message.
        </p>

Change 3 — find this exact block:
          <ul
            className="text-[20px] max-w-[480px] mx-auto mt-3"
            style={{ color: 'rgba(249,245,239,0.72)', lineHeight: 1.8, listStyle: 'none', padding: 0 }}
          >
            {['Hand-picked experts', 'Evidence-based insights', 'Compelling stories', 'A clear narrative arc'].map((item) => (
              <li key={item} style={{ display: 'flex', alignItems: 'baseline', gap: '8px', justifyContent: 'center' }}>
                <span style={{ color: '#ff7f29', flexShrink: 0 }}>✓</span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
Replace with:
          <p
            className="text-[20px] max-w-[480px] mx-auto mt-3"
            style={{ color: 'rgba(249,245,239,0.72)', lineHeight: 1.6 }}
          >
            From the experts and evidence-based insights you bring into the story, every detail stays true to your message.
          </p>

Do not touch the headline above any of these three blocks, the photo, any breakpoint, or any other part of this file.
File: src/components/sections/faq.tsx
Replace the entire contents of this file with the code below. Do not change the file path or the component name. This adds the comparison table you previously had in the "Serious About Series" section as a new first item in the FAQ accordion, with all of its original data and interactive behavior preserved exactly, and keeps every existing FAQ question and answer unchanged.

[paste the full faq.tsx content from above]

This must render correctly at all breakpoints: below 768px, the 768px md breakpoint, and the 1200px lg breakpoint. The five existing FAQ questions and their answers must remain word-for-word identical to what's there now. Do not change anything else in the project.