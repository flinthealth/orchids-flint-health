Now I have everything I need. Here's exactly what's happening and why it's broken:
Each panel has two spacer divs at the top — h-[76px] (header height) and h-[56px] (tab bar height) — then justify-start pt-16 for the content. So the content sits near the top of the panel, and the bottom ~400px of each 100vh panel is empty.
The container is already md:h-[300vh] which is correct. The panels are md:sticky md:top-0 which is also correct in theory. The bug is top-0. All three panels are trying to stick at the very top of the viewport simultaneously. They need to stick at top-0 relative to their own scroll territory but the container needs to be restructured so each panel has its own wrapper.
Here's the Orchids prompt:

File to edit: the ProcessSection component (look for the element with class md:h-[300vh] containing the three panels with ids process-strategy, process-production, process-growth)
What to change — structural only, do not touch any copy, testimonials, or bullet points:
Replace the current panel container structure with a wrapper-per-panel approach. Currently all three panels sit directly inside one md:h-[300vh] div and all use md:sticky md:top-0. This makes them stack on top of each other.
Change to this structure:
Each panel (#process-strategy, #process-production, #process-growth) needs its own outer wrapper div with class="md:h-screen". The panel itself moves inside that wrapper and becomes md:sticky md:top-[132px] md:h-[calc(100vh-132px)] (132px = 76px header + 56px tab bar). Remove the two spacer divs (h-[76px] and h-[56px]) that are currently inside each panel — the top-[132px] offset replaces them.
The content div inside each panel currently has justify-start pt-16 — change that to justify-center py-12 so content sits vertically centered in the pinned viewport.
The overall container that currently has md:h-[300vh] keeps that class — do not change it.
So the final structure looks like:
<div class="md:h-[300vh]">        ← keep as-is
  <div class="md:h-screen">       ← NEW wrapper for panel 1
    <div id="process-strategy" class="md:sticky md:top-[132px] md:h-[calc(100vh-132px)] bg-white md:overflow-hidden">
      <div class="flex flex-col h-full justify-center py-12 max-w-[1000px] mx-auto px-6 md:px-8 w-full">
        ... existing content unchanged ...
      </div>
    </div>
  </div>
  <div class="md:h-screen">       ← NEW wrapper for panel 2
    <div id="process-production" ...same classes...>
      ... existing content unchanged ...
    </div>
  </div>
  <div class="md:h-screen">       ← NEW wrapper for panel 3
    <div id="process-growth" ...same classes...>
      ... existing content unchanged ...
    </div>
  </div>
</div>
Do not change the sticky tab bar, the overview cards, the section header, or any text content anywhere in the component.