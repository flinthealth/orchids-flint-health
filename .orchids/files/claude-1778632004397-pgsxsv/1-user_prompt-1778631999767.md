
Content Bridge — Desktop Direction 2 Final (content-bridge.tsx)
Replace the entire @media (min-width: 1200px) block with this:
css@media (min-width: 1200px) {
  .cb-photo-wrapper { display: contents; }

  .cb-photo {
    height: 60vh;
    width: 100%;
    max-width: none;
    margin: 0;
  }

  .cb-bleed {
    position: absolute; inset: 0;
    height: 100%;
    background: none;
    -webkit-mask-image: none;
    mask-image: none;
    pointer-events: none;
  }

  .cb-vignette-sides { display: block; }
  .cb-vignette-bottom { display: block; }

  .cb-vignette-sides {
    position: absolute;
    top: 0; left: 0; right: 0; bottom: -80px;
    background:
      linear-gradient(to right, #3d4d58 0%, rgba(61,77,88,0.9) 22%, rgba(61,77,88,0.5) 38%, transparent 50%,
      transparent 50%, rgba(160,82,45,0.5) 62%, rgba(160,82,45,0.9) 78%, #a0522d 100%);
    pointer-events: none;
    z-index: 2;
  }

  .cb-vignette-bottom {
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 65%;
    background: linear-gradient(to bottom,
      transparent 0%,
      rgba(61,77,88,0.15) 30%,
      rgba(61,77,88,0.5) 55%,
      rgba(61,77,88,0.85) 75%,
      #3d4d58 100%
    );
    pointer-events: none;
    z-index: 2;
  }

  .cb-text-on-photo {
    display: block;
    position: absolute;
    bottom: 28px; left: 0; right: 0;
    padding: 0 80px;
    text-align: left;
    z-index: 20;
  }

  .cb-text-on-photo h2 {
    font-size: 52px;
    text-align: left;
    margin: 0 0 12px;
    max-width: 680px;
  }

  .cb-text-on-photo p {
    font-size: 20px;
    text-align: left;
  }

  .cb-continuation {
    display: block;
    background: none;
    margin-top: 0;
    padding: 24px 80px 52px;
    text-align: left;
  }

  .cb-continuation p {
    font-size: 18px;
    max-width: 520px;
    margin: 0;
  }

  .cb-text-desktop { display: none; }
}
Add to default (mobile-first) CSS:
css.cb-vignette-sides { display: none; }
.cb-vignette-bottom { display: none; }
And add both divs inside the photo wrap in JSX — right after the existing .cb-bleed div:
jsx<div className="cb-vignette-sides" />
<div className="cb-vignette-bottom" />
Also update the <img> element to include lg:object-[center_20%] so the crop shows your face and upper body on desktop.
Desktop only. Mobile and tablet unchanged.
