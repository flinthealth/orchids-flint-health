File: src/components/sections/radar-dial.tsx
Do not change anything else in this file. Make these four changes only.

Change 1 — find this exact line:
const SPEED = 0.7;
Replace with:
const SPEED = 0.35;

Change 2 — find this exact block inside drawBackground():
      [0.25, 0.5, 0.75, 1.0].forEach(f => {
        ctx!.beginPath();
        ctx!.arc(cx, cy, R * f, 0, Math.PI * 2);
        ctx!.strokeStyle = f === 1 ? 'rgba(61,77,88,0.8)' : 'rgba(61,77,88,0.25)';
        ctx!.lineWidth = f === 1 ? 1.5 : 0.75;
        ctx!.stroke();
      });
Replace with:
      [0.25, 0.5, 0.75, 1.0].forEach(f => {
        ctx!.beginPath();
        ctx!.arc(cx, cy, R * f, 0, Math.PI * 2);
        ctx!.strokeStyle = f === 1 ? 'rgba(255,127,41,0.5)' : 'rgba(84,129,154,0.45)';
        ctx!.lineWidth = f === 1 ? 1.5 : 1;
        ctx!.stroke();
      });

Change 3 — find the entire drawLabels function and replace it with an empty function:
    function drawLabels() {
      ctx!.font = '600 9px sans-serif';
      ctx!.textAlign = 'center';
      CHANNELS.forEach(ch => {
        const a = toRad(ch.angle);
        const lr = R + 22;
        const x = cx + Math.cos(a) * lr;
        const y = cy + Math.sin(a) * lr;
        ctx!.fillStyle = ch.color;
        ctx!.shadowColor = ch.color;
        ctx!.shadowBlur = 4;
        ctx!.fillText(ch.label.toUpperCase(), x, y + 4);
        ctx!.shadowBlur = 0;
      });
    }
Replace with:
    function drawLabels() {
    }

Change 4 — find the center text inside drawCenter():
      ctx!.textAlign = 'center';
      ctx!.font = '300 22px sans-serif';
      ctx!.fillStyle = 'rgba(249,245,239,0.9)';
      ctx!.fillText('360°', cx, cy - 8);
      ctx!.font = '600 8px sans-serif';
      ctx!.fillStyle = 'rgba(200,160,120,0.6)';
      ctx!.fillText('CAMPAIGN REACH', cx, cy + 10);
Replace with:
      ctx!.textAlign = 'center';
      ctx!.font = '300 28px sans-serif';
      ctx!.fillStyle = 'rgba(249,245,239,0.9)';
      ctx!.fillText('360°', cx, cy + 10);

Do not change anything else in this file.
File: src/components/sections/radar-dial.tsx
Do not change anything else in this file. Find the return statement at the bottom of the component and replace it with:

  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '12px' }}>
      <canvas
        ref={canvasRef}
        width={420}
        height={420}
        style={{ width: '100%', maxWidth: '420px', height: 'auto', display: 'block' }}
      />
      <p style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: '#677283', textAlign: 'center' }}>
        360° Campaigns
      </p>
    </div>
  );

Do not change any other part of this file.
File: src/components/sections/serious-about-series.tsx
Do not change anything else in this file. Find this exact line:
            <h2 className="text-[40px] md:text-[52px] font-light text-[#2b3335] leading-[1.1] tracking-[-0.02em] mb-4">
Replace with:
            <h2 className="text-[28px] md:text-[36px] font-light text-[#2b3335] leading-[1.2] tracking-[-0.01em] mb-4">

Do not change anything else in this file.