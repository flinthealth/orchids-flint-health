File: src/components/sections/series-reach.tsx
Replace the entire contents of this file with the code below. Do not change the file path or the component name. This separates "who do you need to reach" from "what does it cost," moves the cost question below the video and audience rows, and turns the three stats into large rounded Mother Earth cards with colored left bars instead of small tinted boxes attached to each row.

"use client";

import { Heart, Stethoscope, Users, Handshake } from 'lucide-react';

const ROWS = [
  {
    Icon: Heart,
    iconColor: "#eeb20b",
    label: "Patients living with your condition.",
    desc: "Give them the science and the stories that move them from confusion to confidence.",
    stat: "9 in 10",
    statColor: "#eeb20b",
    statDesc: "Patients struggle with health information.",
    cite: "5",
    citeUrl: "https://www.cdc.gov/health-literacy/php/about/tell-others.html",
  },
  {
    Icon: Stethoscope,
    iconColor: "#54819a",
    label: "Providers who need to adopt your protocol.",
    desc: "Meet them in their workflow with content that builds trust and changes practice.",
    stat: "75%",
    statColor: "#54819a",
    statDesc: "Of clinicians overwhelmed by treatment advances.",
    cite: "6",
    citeUrl: "https://www.managedhealthcareexecutive.com/view/survey-reveals-cancer-doctors-struggle-to-keep-up-as-treatments-advance-quickly",
  },
  {
    Icon: Users,
    iconColor: "#9c522e",
    label: "A team that needs to believe in what you're building.",
    desc: "Culture doesn't scale on its own. A series carries your mission to every new hire.",
    stat: "$12K+",
    statColor: "#9c522e",
    statDesc: "Cost of poor communication per employee.",
    cite: "7",
    citeUrl: "https://www.agilitypr.com/pr-news/pr-skills-profession/bad-connection-study-finds-poor-communication-costs-businesses-1-2-trillion-annually/",
  },
  {
    Icon: Handshake,
    iconColor: "#677283",
    label: "Partners and referral networks.",
    desc: "Establish authority before the first meeting with content that speaks for itself.",
    stat: null,
    statColor: null,
    statDesc: null,
    cite: null,
    citeUrl: null,
  },
];

export default function SeriesReach() {
  return (
    <section style={{ background: "#f9f5ef" }} className="w-full pt-24 pb-20 px-6 md:px-12 overflow-hidden">
      <div className="max-w-[1280px] mx-auto">

        {/* Headline */}
        <div className="mb-14 max-w-[700px] mx-auto text-center">
          <h2 className="text-[#2b3335] text-[36px] md:text-[46px] lg:text-[52px] font-light leading-[1.1] tracking-[-0.02em]">
            Who do you need to reach?
          </h2>
        </div>

        <div className="flex flex-col md:flex-row gap-10 md:gap-16 items-start">
          {/* Video */}
          <div className="relative w-full md:w-[38%] flex-shrink-0 rounded-2xl overflow-hidden min-h-[320px] md:min-h-[420px]">
            <video
              autoPlay
              loop
              muted
              playsInline
              className="w-full h-full object-cover"
              src="/stats-idle-time-compressed.mp4"
            />
            <div style={{
              position: 'absolute',
              bottom: 20,
              right: 20,
              zIndex: 20,
              background: 'rgba(255,255,255,0.12)',
              border: '1px solid rgba(255,255,255,0.22)',
              backdropFilter: 'blur(12px)',
              WebkitBackdropFilter: 'blur(12px)',
              borderRadius: 12,
              padding: '14px 18px',
            }}>
              <div style={{ fontSize: 36, fontWeight: 300, color: '#f9f5ef', lineHeight: 1, marginBottom: 4 }}>92%</div>
              <div style={{ fontSize: 11, color: 'rgba(249,245,239,0.75)', lineHeight: 1.4, maxWidth: 140 }}>
                of podcast listeners tune in during their daily routine.
                
                  href="https://www.westwoodone.com/wp-content/uploads/2025/11/Cumulus-Media-and-Signal-Hill-Insights-Podcast-Download-Fall-2025_WWO.pdf"
                  target="_blank"
                  rel="noopener noreferrer"
                  style={{ fontSize: 8, verticalAlign: 'super', marginLeft: 1, opacity: 0.5 }}
                >4</a>
              </div>
            </div>
          </div>

          {/* Audience rows — no stats attached */}
          <div className="flex-1 flex flex-col w-full">
            {ROWS.map(({ Icon, iconColor, label, desc }, i) => (
              <div
                key={label}
                className="flex items-start gap-3 py-5"
                style={{ borderBottom: i < ROWS.length - 1 ? '1px solid rgba(43,51,53,0.1)' : 'none' }}
              >
                <Icon style={{ color: iconColor }} className="flex-shrink-0 mt-[2px]" size={18} />
                <div>
                  <span className="font-semibold text-[17px] text-[#2b3335]">{label}</span>{" "}
                  <span className="font-normal text-[16px] text-[#43382f]">{desc}</span>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Cost question */}
        <div className="mt-16 md:mt-20 mb-8 max-w-[700px] mx-auto text-center">
          <h3 className="text-[#2b3335] text-[28px] md:text-[36px] font-light leading-[1.2] tracking-[-0.01em]">
            <em className="font-serif italic">And what does it cost when you don&rsquo;t connect with them?</em>
          </h3>
        </div>

        {/* Cost pills */}
        <div className="flex flex-col md:flex-row gap-5">
          {ROWS.filter((r) => r.stat).map(({ stat, statColor, statDesc, cite, citeUrl }) => (
            <div
              key={statDesc}
              style={{
                background: '#43382f',
                borderLeft: `5px solid ${statColor}`,
                borderRadius: '20px',
                padding: '28px 32px',
                flex: 1,
              }}
            >
              <div style={{ fontSize: 40, fontWeight: 300, color: '#f9f5ef', lineHeight: 1 }}>{stat}</div>
              <div style={{ fontSize: 14, color: 'rgba(249,245,239,0.65)', lineHeight: 1.4, marginTop: 8 }}>
                {statDesc}
                <a href={citeUrl!} target="_blank" rel="noopener noreferrer"
                  className="align-super ml-0.5 opacity-60 hover:opacity-90 transition-opacity"
                  style={{ fontSize: 10, color: '#f9f5ef' }}>{cite}</a>
              </div>
            </div>
          ))}
        </div>

      </div>
    </section>
  );
}

This must render correctly at all breakpoints: below 768px, the 768px md breakpoint, and the 1200px lg breakpoint. On mobile, the three cost pills should stack vertically rather than compress side by side.